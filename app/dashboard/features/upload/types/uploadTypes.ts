// app/features/upload/types/uploadTypes.ts

export interface UploadResponseData {
  message: string;
  uploadId: string;
  // ... other fields from a successful upload response, if any
}

// Define other types related to upload feature here if needed
